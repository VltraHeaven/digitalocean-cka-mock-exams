# GENERATED BY TERRAFORM — do not edit by hand.
# Regenerate with: terraform apply

[cluster1_cp]
%{ for name, n in control_planes ~}
%{ if n.cluster == "cluster-1" ~}
${name} ansible_host=${n.public_ip} node_ip=${n.private_ip}
%{ endif ~}
%{ endfor ~}

[cluster1_workers]
%{ for name, n in workers ~}
%{ if n.cluster == "cluster-1" ~}
${name} ansible_host=${n.public_ip} node_ip=${n.private_ip}
%{ endif ~}
%{ endfor ~}

[cluster2_cp]
%{ for name, n in control_planes ~}
%{ if n.cluster == "cluster-2" ~}
${name} ansible_host=${n.public_ip} node_ip=${n.private_ip}
%{ endif ~}
%{ endfor ~}

[cluster2_workers]
%{ for name, n in workers ~}
%{ if n.cluster == "cluster-2" ~}
${name} ansible_host=${n.public_ip} node_ip=${n.private_ip}
%{ endif ~}
%{ endfor ~}

[cluster1:children]
cluster1_cp
cluster1_workers

[cluster2:children]
cluster2_cp
cluster2_workers

[control_planes:children]
cluster1_cp
cluster2_cp

[workers:children]
cluster1_workers
cluster2_workers

[cluster1:vars]
cluster_name=cluster-1

[cluster2:vars]
cluster_name=cluster-2

[all:vars]
ansible_user=root
ansible_python_interpreter=/usr/bin/python3
ansible_ssh_private_key_file=${ssh_private_key_path}
ansible_ssh_common_args='-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ConnectTimeout=15'
